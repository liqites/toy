class Combination
	# @param {Integer} n
	# @param {Integer} k
	# @return {Integer[][]}
	# list all possible k-combinations
	# given numbers from 1..n 
	def combine(n, k)
		result = []
		source = Array.new(n) {|i| i+1}
		start = source
		
	end
end